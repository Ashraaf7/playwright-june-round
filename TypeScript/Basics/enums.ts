enum Browser {
    Chrome,
    Firefox,
    Safari,
    Edge
}

console.log(Browser.Chrome);

enum environment {
    dev = "www.dev.com",
    test = "www.test.com",
    preprod = "www.preprod.com",
    prod = "www.prod.com",
    age = 30
}

"egypt Egypt"

console.log(environment.dev);


